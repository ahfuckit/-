```JavaScript

const settings = {
console: console || {},
that: this
}
function start(settings = settings) {
class InitLogs {
  constructor() {
  const inject = this;
  Object.assign(this, inject, Window);
this.inject = settings;
}

const init = () => {return new InitLogs()};
init();
}})()
```