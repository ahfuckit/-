```JavaScript

const settings = {
console: console,
that: this
}

class InitLogs {
  constructor() {
  const inject = this.settings;
  Object.assign(this, inject, Window);
this.inject = settings;
}

}
```